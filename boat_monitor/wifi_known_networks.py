# Known Wi-Fi networks shipped with firmware (OTA-updatable via ota_manifest.json).
# Merged with wifi_credentials.WIFI_NETWORKS on the Pico (device secrets).
# Put passwords only in wifi_credentials.py (gitignored); entries here may be
# (ssid, password) when you are comfortable baking them into a private repo build.

WIFI_NETWORKS = []
